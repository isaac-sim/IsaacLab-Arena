**/.mypy_cache
**/__pycache__
/.git
/.agents
/.claude
# Isaac Sim crash core dumps — ~3GB each; otherwise pulled into the image by `COPY *.*`
/core.*
# uv-managed virtualenvs (the native install creates .venv); never ship them
# into the Docker build context.
/.venv*
