:orphan:

Sensitivity Analysis
====================

The workflow is now split between :doc:`../analysis/variations`, which collects episode results,
and :doc:`Sensitivity Analysis <index>`, which generates and interprets the report.
