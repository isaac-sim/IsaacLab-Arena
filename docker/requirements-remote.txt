# Remote policy infrastructure dependencies.
# Shared by Dockerfile.isaaclab_arena and Dockerfile.gr00t_server.
#
# This file intentionally keeps only CUDA-agnostic requirements.
# CUDA-coupled wheels (for example `ucx-py-cu12` or its `cu13` variant)
# are installed in the Dockerfiles after detecting the CUDA major version
# from the image's Python/Torch runtime.
pyzmq
msgpack
